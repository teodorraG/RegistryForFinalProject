@model RegistryForFinalProject.Models.ViewModels.LogInViewModel
@{
    ViewData["Title"] = "LogIn";
}

<h1>Log In</h1>
<form asp-controller="Account">
    <label action="/Account/LogIn" asp-for="UserName">Username: </label>
    <span class="text-danger">@Html.ValidationMessageFor(m => m.UserName)</span>
    <input asp-for="UserName" class="form-control inputSizeTwenty" />
    <label action="/Account/LogIn" asp-for="Password">Password: </label>
    <span class="text-danger">@Html.ValidationMessageFor(m => m.Password)</span>
    <input asp-for="Password" class="form-control inputSizeTwenty" type="password" />
    <br />
    <br />
    <button type="submit" class="btn-primary" id="LogInButton">Log In</button>

    <a id="myFormSubmit" href="ForgottenPassword">Forgot your password?</a>
</form>
<script src="~/js/site.js"></script>

@section MainNavigation {

    @if (HttpContextAccessor.HttpContext.Session.GetString("CurrentUser") != null)
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="" asp-controller="Registry" asp-action="Registry">Registry</a>
        </li>
    }
}

@section AccountNavigation {
    @if (HttpContextAccessor.HttpContext.Session.GetString("CurrentUser") != null)
    {
        <li>
            <a class="nav-link text-dark" asp-area="" asp-controller="Profile" asp-action="Profile"> My Profile</a>
        </li>
        <li>
            <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="LogOut">Log out</a>
        </li>

    }
    else
    {
        <li>
            <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="LogIn">Log in</a>
        </li>
        <li>
            <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="Register">Register</a>
        </li>
    }
}